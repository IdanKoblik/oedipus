name: CI

on:
  workflow_dispatch:
    inputs:
      VERSION:
        description: 'Version to upload'
        required: true

  pull_request:
  push:
    branches:
      - main

jobs:
  publish:
    needs: testing
    uses: ./.github/workflows/callable.publish.yml
    with:
      VERSION: ${{ inputs.VERSION }}